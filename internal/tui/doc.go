// Package tui is a reserved boundary for future presentation-only terminal UI
// helpers.
//
// SQLcery's live Bubble Tea application remains in internal/app for now. The
// current UI is deeply intertwined with application state, query execution, and
// mode coordination, so moving it into this package today would mostly rename
// files without creating a cleaner boundary.
package tui
