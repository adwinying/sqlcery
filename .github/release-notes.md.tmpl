## Highlights

- Summarize the user-facing changes in this release.
- Call out any migration notes, known limitations, or breaking behavior.

## Installation

- `go install github.com/adwinying/sqlcery/cmd/sqlcery@{{ .Tag }}`
- Download the matching archive for your platform from this release's assets.

## Changelog

{{ .ReleaseNotes }}

## Verification

- Checksums for all release artifacts are published in `checksums.txt`.
- Verify a download with `sha256sum -c checksums.txt` or `shasum -a 256 -c checksums.txt`.
